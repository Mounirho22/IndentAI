# docs/

Internal development documentation.

- `ARCHITECTURE.md` - system architecture, provider abstraction, honest notes.
- `AGENT_SYNC.md` - live tracking document for feature/compliance status.
- `hud_icons_preview.html` - standalone preview of the animated review-desk
  icons (open directly in a browser; the real icons are embedded in
  `app/main.py`).
